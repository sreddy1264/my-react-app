

funcOne('one') // => ?
funcTwo('two') // => ? 

function funcOne (str) {
  console.log(str)} 
var funcTwo = function (str) {
  console.log(str)}


  //

    (function() {​
        height = 72

  var width = 180
}​)()

console.log(height)// => ?
console.log(width)// => ?

repeat('word') // => 'wordword'

// req 2: repeat('word', 3) // => 'wordwordword' repeat('word', 1) // => 'word' repeat('word', 0) // => ''

const repeat = (str, num) => {
    let word;
    if(num > 0) {
       word = str * num;
    } else {
        word = '';
    }
    return word;
}

// req: 3 < 20 and sort by 

const pets = [  
    { name: 'Whiskers', age: 19 },  
    { name: 'Timothy', age: 25 }, 
     { name: 'Sammy', age: 7 },  
     { name: 'Johnny', age: 21 }]

   const sortByAge = pets.filter((pet) => pet.age > 20);

   sortByAge.sort(pets.name);
const reverseAString = (str) => str.split('').reverse().join('');

//prime number

const isAPrimeNumber = (num) => num % 
    let primen
    if(num) {
        
    }
} 
